# bootstrap 上下文配置

# 配置服务器 URl
spring.cloud.config.uri=http://localhost:10000
# 配置客户端应用名称:{application}
spring.cloud.config.name = darian
# profile 是激活配置
spring.cloud.config.profile = test
# label 在Git中指的分支名称
spring.cloud.config.label = master 